# Upstream

Repositorios de origem, ambientes Conda e comandos dos modelos do artigo.

```bash
make setup
make train MODEL=cascade
```

Modelos em `upstream/commands.yaml`.

ESFPNet usa o repo oficial em `upstream/vendor/esfpnet` com overlay CLI para o fluxo Kvasir.
